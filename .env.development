# .env.development（根目录下，单独文件，每行一个变量，无多余代码）
# 前端请求基础URL
VUE_APP_BASE_API=/api
# 后端代理目标地址（必须是完整URL，包含http:// + IP + 端口）
VUE_APP_PROXY_TARGET=http://192.168.146.129:8080
# VUE_APP_PROXY_TARGET=http://127.0.0.1:8080
# 项目标题（单独成行，无代码拼接）
NODE_ENV=development
VUE_APP_TITLE=OJ平台 - 开发环境
# 手动控制是否开启 Mock (true: 开启, false: 关闭)
VITE_USE_MOCK=false
